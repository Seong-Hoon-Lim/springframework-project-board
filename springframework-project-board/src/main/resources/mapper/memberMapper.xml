<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.springframeworkprojectboard.mapper.MemberMapper">

    <insert id="save">
        insert into spring_board.member (memberId, password, name, gender, birth, email, phone, zipcode, addr1, addr2, created_at)
        values (#{memberId}, #{password}, #{name}, #{gender}, #{birth}, #{email}, #{phone}, #{zipcode}, #{addr1}, #{addr2}, now())
    </insert>

</mapper>